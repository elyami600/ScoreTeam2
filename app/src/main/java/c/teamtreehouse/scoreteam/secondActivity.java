package c.teamtreehouse.scoreteam;import android.content.Intent;import android.os.Bundle;import android.support.v7.app.AppCompatActivity;import android.util.Log;import android.view.View;import android.widget.TextView;public class secondActivity extends AppCompatActivity {    private TextView tv_display;    private TextView tvScoreWinner;    private static final String TAG = "secondActivity";    public static String strWinnerTeam=" ";    public static String winner="winner";    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_second);        Log.d(TAG, "inside onCreate");        tv_display    = (TextView)findViewById(R.id.tv_display2);        tvScoreWinner = (TextView)findViewById(R.id.tvScoreWinner);        //Get intent from he originating activity be sure to use the correct key to get the value        Intent intent = getIntent();        String strScoreCount = intent.getExtras().get(MainActivity.KEY_SCORE).toString();        strWinnerTeam = intent.getExtras().get(MainActivity.WINNER_TEAM).toString();        tv_display.setText(strScoreCount);        tvScoreWinner.setText(strWinnerTeam);        Log.d(TAG, "end of onCreate");    }    public void gotoOtherActivity(View view) {        Log.d(TAG, "inside gotoOtherActivity ");        Intent intent = new Intent(this,OtherActivity.class);        intent.putExtra(winner, strWinnerTeam);        startActivity(intent);    }}